spring:
  application:
    name: learn-spring-ai

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.2:3b
      embedding:
        options:
          model: nomic-embed-text
    vectorstore:
      pgvector:
        initialize-schema: true
        enabled: true
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always
            table-name: chat_memory

  datasource:
    url: jdbc:postgresql://localhost:5431/postgres
    username: postgres
    password: mysecretpassword
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true


#    openai:
#      api-key: gfdgdfsgfdshhjhjjhhghghghgffkkjjjnnnkjj-jjhjhjhhjjj-kjknnunnuuyyoom
#      chat:
#        options:
#          model: gpt-4o-mini
#          temperature: 0.9

logging:
  level:
    org.springframework.ai:
      chat.client: DEBUG
